# Crypto Signal Bot — secrets (copy to .env and fill in)
# Public Binance market data needs NO API keys.

# Telegram bot token from @BotFather (single bot only)
TELEGRAM_TOKEN=

# Telegram chat id that receives alerts (from @userinfobot or getUpdates)
TELEGRAM_CHAT_ID=

# AI provider API key (AgentRouter: https://agentrouter.org — the OPENROUTER_API_KEY
# name is kept for compatibility; any OpenAI-compatible provider works)
OPENROUTER_API_KEY=

# Optional overrides (defaults below)
# AI_BASE_URL=https://agentrouter.org/v1
# AI_MODEL=deepseek-v4-flash
# AI_MODEL_FALLBACK=            # empty by default: set a second model when the provider has one
# AI_DAILY_BUDGET=50
# LOG_LEVEL=INFO
